export enum UserStatus {
    Inactive = 0,
    Active = 1,
    Banned = 2,
}

export enum RoleCode {
    Admin = 'ADMIN',
    Manager = 'MANAGER',
    Staff = 'STAFF',
    User = 'USER',
}

export enum BorrowStatus {
    Active = 'active',
    Returned = 'returned',
    Overdue = 'overdue'
}